{
  "name": "second-page",
  "private": true,
  "scripts": {
    "build": "esbuild second.ts --bundle --format=esm --target=es2020 --outfile=second.js",
    "watch": "esbuild second.ts --bundle --format=esm --target=es2020 --outfile=second.js --watch"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
